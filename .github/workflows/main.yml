on:
  push:
    branches:
      - master

 

 jobs:
   build:
     runs-on: ubuntu-latest


     steps:
       - name: 'copy all'
         run: ls


      #  - name: 'Testing Printing'
      #    run: echo Rahma